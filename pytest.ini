[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    live: hits live external services (skipped in CI via `-m "not live"`)
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
