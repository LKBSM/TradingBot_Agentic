# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Re-include Next.js webapp lib/ (Python `lib/` rule above otherwise hides it)
!/webapp/lib/
!/webapp/lib/**

# Virtual environments
venv/
ENV/
env/
.venv/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Data files (large files - use git lfs or keep in releases/tags)
data/*.csv
data/*.parquet
data/*.h5
data/*.db
!data/.gitkeep

# Kronos model repo (cloned locally for POC)
kronos_repo/

# POC results
poc_results/

# Models (large files)
models/
trained_models/
*.zip
*.h5
*.pkl
*.joblib

# Logs
logs/
*.log
tensorboard_logs/

# Environment variables
.env
.env.local
.env.*.local

# Security - salt/key files
.trading_bot_salt

# OS
.DS_Store
Thumbs.db
nul

# Results
results/

# Temporary
tmp/
temp/
*.tmp

# Backups (Sprint 0 institutional overhaul)
backups/

# Stash recovery 2026-05-30 — ignore execution artefacts
replay_*.json
replay_*.csv
.coverage
*.coverage.*
backtest_trades.csv
.claude/scheduled_tasks.lock

# Chantier 1 — MarketReadingsStore SQLite (WAL companions)
data/market_readings.db
data/market_readings.db-shm
data/market_readings.db-wal

# Chantier 1 — CandlesCacheStore SQLite (WAL companions)
data/candles.db
data/candles.db-shm
data/candles.db-wal

# Node — root-level node_modules (build/test caches, never tracked)
node_modules/

# ── ORD-1 : artefacts jetables (captures de test, sorties de build, dumps) ──
# Motifs CIBLÉS et ancrés à la racine — jamais une extension entière (*.png…),
# jamais docs/ webapp/src/ ni scripts/ hors caches. __pycache__/ déjà couvert.
# Captures d'écran de debug (racine)
/app_chat_live.png
/app_chat_reply.png
/app_chat_top.png
/app_home.png
/img.png
/scanner_results.png
# Captures d'écran de debug (webapp)
/webapp/run_shot.png
/webapp/shot_h4_chart_after_fix.png
/webapp/shot_h4_left_edge_after_fix.png
/webapp/shot_h4_left_zoom.png
/webapp/shot_scanner_1.png
/webapp/shot_scanner_results.png
# Sorties de build / tsc / backend capturées
/be_out.txt
/fe_build2.txt
/fe_t.txt
/fe_tsc2.txt
/fe_tsc3.txt
# Scripts jetables
/drive_tmp.mjs
/webapp/drive_scanner.mjs
/webapp/shot.mjs
/test_env_debug.py
# Dumps / notes de debug
/mr.json
/MT-D1_calcul_tendance.txt
# Script d'entraînement Colab (perso, hors dépôt)
/Script collab
# Données volumineuses régénérables (snapshots fournisseur, ~38 Mo de .jsonl)
/data/provider_snapshots/
# Cache régénérable de l'échantillon MT-D1 (le livrable lui-même est versionné)
/docs/audits/ECHANTILLON-DETECTION-2026-07-29/_cache/
